<h2><%= @task.title %></h2>
<p><%= @task.details %></p>
 <p class="%= @task.completed ? 'task-completed' : 'task-incomplete'"><%= @task.completed ? 'Completed' : 'Incomplete' %></p>
<a class="button" href="<%= edit_task_path(@task) %>">Edit</a>
<a class="button" href="<%= root_path %>">Back</a>
